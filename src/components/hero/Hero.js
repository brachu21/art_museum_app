import styled from 'styled-components';

const Hero = styled.img`
  width: 100vw;
  height: auto;
  position: relative;
  left: 50%;
  right: 50%;
  margin-left: -50vw;
  margin-right: -50vw;
  z-index: 1;
`;

export default Hero;

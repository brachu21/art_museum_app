import React from 'react';

const NotFound = () => (
    <div style={{display: 'flex', flexDirection: 'column', alignItems: 'center', alignContent: 'center'}}>
        <h1>Page not developed</h1>
        <h1>for trial purposes</h1>
    </div>
);

export default NotFound;
